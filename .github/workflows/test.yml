name: 'units-test'
on:
  pull_request:
  push:
    branches:
      - main
      - 'releases/*'

permissions:
  contents: read

jobs:
  # unit tests
  units:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          persist-credentials: false

      - name: Setup Node.js
        id: setup-node
        uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f # v6
        with:
          node-version-file: .node-version
          cache: npm

      - run: npm ci
      - name: Run tests
        run: |
          npx vitest run --coverage 2>&1 | tee ./coverage.txt

      - name: Get branch name
        id: branch-name
        uses: tj-actions/branch-names@5250492686b253f06fa55861556d1027b067aeb5 # v9

      - name: Vitest Coverage Comment
        id: coverageComment
        uses: MishaKav/jest-coverage-comment@c480abe47ef4f7bcd87bd8b5e6043fdfb1c4d270 # main
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          summary-title: Vitest Coverage (summary)
          title: Test Coverage
          coverage-summary-path: coverage/coverage-summary.json
          coverage-path: ./coverage.txt

      - name: Check the output coverage
        run: |
          echo "Coverage Percentage - ${STEPS_COVERAGECOMMENT_OUTPUTS_COVERAGE}"
          echo "Coverage Color - ${STEPS_COVERAGECOMMENT_OUTPUTS_COLOR}"
          echo "Summary HTML - ${STEPS_COVERAGECOMMENT_OUTPUTS_SUMMARYHTML}"
          mv coverage/coverage-summary.json .
        env:
          STEPS_COVERAGECOMMENT_OUTPUTS_COVERAGE: ${{ steps.coverageComment.outputs.coverage }}
          STEPS_COVERAGECOMMENT_OUTPUTS_COLOR: ${{ steps.coverageComment.outputs.color }}
          STEPS_COVERAGECOMMENT_OUTPUTS_SUMMARYHTML: ${{ steps.coverageComment.outputs.summaryHtml }}

      - name: Create coverage badge
        if: steps.branch-name.outputs.current_branch == 'main'
        run: |
          COVERAGE=${STEPS_COVERAGECOMMENT_OUTPUTS_COVERAGE}
          COLOR=${STEPS_COVERAGECOMMENT_OUTPUTS_COLOR}
          mkdir -p badges
          curl -s "https://img.shields.io/badge/Test%20Coverage-${COVERAGE}%25-${COLOR}" > badges/coverage.svg
        env:
          STEPS_COVERAGECOMMENT_OUTPUTS_COVERAGE: ${{ steps.coverageComment.outputs.coverage }}
          STEPS_COVERAGECOMMENT_OUTPUTS_COLOR: ${{ steps.coverageComment.outputs.color }}

      - name: Commit coverage badge and summary
        if: steps.branch-name.outputs.current_branch == 'main'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "github-actions@github.com"
          git remote set-url origin "https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          mkdir -p data
          cp coverage-summary.json data/coverage-summary.json
          git add badges/coverage.svg data/coverage-summary.json
          if ! git diff --staged --quiet; then
            git commit -m "Update coverage badge and summary [skip ci]"
            git push
          fi

      - name: Get main coverage for diff
        if: steps.branch-name.outputs.current_branch != 'main'
        run: |
          curl -sf "https://raw.githubusercontent.com/${{ github.repository }}/main/data/coverage-summary.json" -o main-coverage.json || echo '{"total":{"lines":{"pct":0}}}' > main-coverage.json
          function diff {     diff="$(echo "$2" - "$1" | bc -l | sed -e 's/^\./0./' -e 's/^-\./-0./')" ;    echo "$diff"; }
          current=$(jq .total.lines.pct coverage-summary.json)
          main=$(jq .total.lines.pct main-coverage.json)
          pctLineChange=$(diff "$main" "$current")
          echo '**Code Coverage change**' > coverage.md
          echo 'This PR changes coverage by' "$pctLineChange" '%' >> coverage.md
          echo 'Component coverage will change from ' "$main"'% to ' "$current"'%.' >> coverage.md
          echo "$(<coverage.md)"

      - name: Add PR Comment with coverage diff
        if: steps.branch-name.outputs.current_branch != 'main'
        uses: mshick/add-pr-comment@64b8e914979889d746c99dea15a76e77ef64580a # v3.10.0
        with:
          message-path: coverage.md
          update-only: false
          refresh-message-position: true
          allow-repeats: false
          message-id: 'add-pr-diff-comment-${{ github.event.pull_request.number }}'

  # test action works running from the graph
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          persist-credentials: false
      - uses: ./
        id: dora
        with:
          owner: stenjo
          repo: '[dora,Middager]'
          token: ${{ secrets.GITHUB_TOKEN }}
          logging: true
          filtered: true

      # Use the output from the `dora` step
      - name: Get the output rate
        run: |
          printf "The deploy rate was %s" "${STEPS_DORA_OUTPUTS_DEPLOY_RATE}"
          printf "The log was:\n%s" "${STEPS_DORA_OUTPUTS_DEPLOY_RATE_LOG}"
          printf "%s" "${STEPS_DORA_OUTPUTS_DEPLOY_RATE_LOG}" > deploy-rate-log.txt
          echo "The lead time was ${STEPS_DORA_OUTPUTS_LEAD_TIME}"
        env:
          STEPS_DORA_OUTPUTS_DEPLOY_RATE: ${{ steps.dora.outputs.deploy-rate }}
          STEPS_DORA_OUTPUTS_DEPLOY_RATE_LOG: ${{ steps.dora.outputs.deploy-rate-log }}
          STEPS_DORA_OUTPUTS_LEAD_TIME: ${{ steps.dora.outputs.lead-time }}

      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: logs
          path: ./*-log.txt
